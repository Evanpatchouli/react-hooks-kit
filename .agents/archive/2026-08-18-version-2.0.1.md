# v2.0.1 版本推进

## 结果

- 统一根项目、源码包、发布包和锁文件根元数据到 `2.0.1`。
- 同步三份 README 的版本徽章与 Report、源码/发布包 ChangeLog。
- 重建 `docs`，运行时版本变为 `2.0.1`，并保留 `/react-hooks-kit/` GitHub Pages 资源基路径。

## 验证

- `npm run build` 通过。
- `npx tsc -p tsconfig.json --noEmit --pretty false` 通过。
- `npm run test:run`：74 个测试文件、468 个测试通过。
- 文档入口引用和 UTF-8 无 BOM 检查通过。

## 限制

- `npm run package` 在 Windows 清理既有 `dist` 目录时因 `EPERM` 失败；未删除现有发布目录，已直接同步其版本元数据。
- 未执行 Git push。
